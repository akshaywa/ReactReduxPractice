export interface FormState {
    studentName: string;
    dob: Date;
    schoolName: string;
    rollNumber: number;
    email: string;
    phoneNumber: number;
    image: string | ArrayBuffer | null;
}